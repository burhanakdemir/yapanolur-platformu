/** Ortak yukleme sinirlari — /api/uploads ve /api/register/uploads ile hizali. */
export const MAX_UPLOAD_BYTES = 8 * 1024 * 1024;

export const ALLOWED_UPLOAD_EXTENSIONS = new Set([".jpg", ".jpeg", ".png", ".webp", ".pdf"]);
